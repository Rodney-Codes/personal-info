# Portfolio (planned)

Hosted portfolio site will live here. **Use `content/resume.md` as the single source of truth** for resume copy: import or fetch that file at build time (e.g. static site generator, Next.js content layer, or CI step that copies it into `public/`).

The PDF resume is produced separately by `resume_pdf/` and written to `artifacts/resume.pdf`.
